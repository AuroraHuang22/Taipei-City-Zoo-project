import React, { useState, useEffect, useMemo } from "react";
import styled from "styled-components";

const AnimalsDiv = styled.div`
  heigh: 100%;
  padding: 20px;
`;

const Animals = () => {
  return <AnimalsDiv>c</AnimalsDiv>;
};

export default Animals;
